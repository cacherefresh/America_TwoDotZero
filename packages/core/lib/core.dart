library core;

/// A simple utility example from the core package.
class CoreUtils {
  /// Returns a welcome message that can be shared across apps.
  static String welcomeMessage() => 'Welcome from core!';
}
